import { Button } from "@/components/ui/button";
import React from "react";

const Home = () => {
  return (
    <div>
      index <Button variant="default"> Hola</Button>
    </div>
  );
};

export default Home;
